/**
 * Built-in vermilion theme presets: see `pptx-viewer-shared`.
 *
 * Re-exported here so `./theme` import paths stay stable.
 */
export {
	vermilionLightColors,
	vermilionDarkColors,
	vermilionLightTheme,
	vermilionDarkTheme,
	vermilionRadius,
} from 'pptx-viewer-shared';
