/**
 * Thin re-export shim. The framework-agnostic tab-leader helpers now live in
 * `pptx-viewer-shared`.
 */
export { getLeaderCharacter, getLeaderStyles, buildLeaderString } from 'pptx-viewer-shared';
