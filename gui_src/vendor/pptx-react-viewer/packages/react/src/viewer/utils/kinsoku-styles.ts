/**
 * Thin re-export shim. The framework-agnostic kinsoku (CJK line-breaking) CSS
 * helper now lives in `pptx-viewer-shared`.
 */
export { getKinsokuLineBreakStyles } from 'pptx-viewer-shared';
export type { KinsokuStyle } from 'pptx-viewer-shared';
